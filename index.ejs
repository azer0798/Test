<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>WassitDZ Game | متجر الحسابات</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.rtl.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Cairo:wght@400;600;700&display=swap" rel="stylesheet">
    
    <style>
        :root { --primary: #ff4d94; --bg-body: #fff5f8; --card-bg: #ffffff; }
        body { background: var(--bg-body); font-family: 'Cairo', sans-serif; padding-top: 40px; margin: 0; overflow-x: hidden; }

        /* شريط الإعلان المستمر */
        .top-notice { background: var(--primary); color: #fff; height: 40px; position: fixed; top: 0; width: 100%; z-index: 2000; overflow: hidden; display: flex; align-items: center; }
        .scrolling-text { display: inline-block; white-space: nowrap; animation: continuous-scroll 20s linear infinite; font-size: 13px; font-weight: bold; }
        @keyframes continuous-scroll { 0% { transform: translateX(100%); } 100% { transform: translateX(-100%); } }

        /* الهيدر */
        .header-main { background: rgba(255, 255, 255, 0.9); backdrop-filter: blur(10px); padding: 10px 20px; border-bottom: 2px solid #ffe6f0; display: flex; align-items: center; justify-content: space-between; position: sticky; top: 40px; z-index: 1000; flex-direction: row-reverse; }
        .header-main img { max-height: 55px; }
        .menu-btn { background: #fff; border: 2px solid #ffe6f0; padding: 8px 12px; border-radius: 12px; cursor: pointer; color: var(--primary); font-size: 18px; }

        /* محول العملات */
        .currency-card { background: #fff; border-radius: 20px; border: 2px dashed #ffe6f0; padding: 20px; margin-bottom: 30px; transition: 0.3s; }
        .currency-card:hover { border-style: solid; box-shadow: 0 10px 20px rgba(255, 77, 148, 0.05); }

        /* البطاقات */
        .account-card { background: var(--card-bg); border-radius: 18px; border: 2px solid #ffeef4; overflow: hidden; transition: 0.3s ease; margin-bottom: 20px; }
        .account-card:hover { transform: translateY(-5px); border-color: var(--primary); }
        .price-tag { color: var(--primary); font-weight: 800; }
        .btn-main { background: var(--primary); border: none; border-radius: 10px; color: #fff; font-weight: 700; width: 100%; padding: 8px; }

        /* القائمة الجانبية */
        .sidebar { position: fixed; top: 0; right: -280px; width: 280px; height: 100%; background: #fff; z-index: 3000; transition: 0.4s ease; padding: 25px; box-shadow: -5px 0 20px rgba(0,0,0,0.05); }
        .sidebar.active { right: 0; }
        .sidebar-overlay { position: fixed; top: 0; left: 0; width: 100%; height: 100%; background: rgba(0,0,0,0.3); z-index: 2999; display: none; }
        .sidebar-overlay.active { display: block; }
        .sidebar-link { display: flex; align-items: center; gap: 12px; padding: 12px; color: #555; text-decoration: none; font-weight: 600; border-radius: 10px; margin-bottom: 8px; }
        .sidebar-link i { color: var(--primary); width: 30px; text-align: center; }

        /* الأسئلة الشائعة */
        .faq-item { border-bottom: 1px solid #f9f9f9; padding: 10px 0; }
        .faq-question { font-weight: 700; color: var(--primary); cursor: pointer; display: flex; justify-content: space-between; }
        .faq-answer { max-height: 0; overflow: hidden; transition: 0.4s; font-size: 14px; color: #777; }
        .faq-item.active .faq-answer { max-height: 200px; padding: 10px 0; }

        footer { text-align: center; padding: 40px; color: #aaa; font-size: 13px; }
    </style>
</head>
<body>

    <div class="top-notice">
        <div class="scrolling-text">
            <i class="fas fa-bullhorn me-2"></i> <%= settings.announcement %> &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp; <i class="fas fa-bullhorn me-2"></i> <%= settings.announcement %>
        </div>
    </div>

    <header class="header-main">
        <img src="<%= settings.logoUrl %>" alt="WassitDZ">
        <button class="menu-btn" onclick="toggleMenu()"><i class="fas fa-bars-staggered"></i></button>
    </header>

    <div class="sidebar-overlay" id="overlay" onclick="toggleMenu()"></div>
    <div class="sidebar" id="sidebar">
        <h5 class="fw-bold mb-4" style="color:var(--primary)">القائمة</h5>
        <a href="/" class="sidebar-link"><i class="fas fa-home"></i> الرئيسية</a>
        <a href="<%= settings.mediationLink %>" class="sidebar-link"><i class="fas fa-handshake"></i> طلب وساطة</a>
        <a href="<%= settings.sellAccountLink %>" class="sidebar-link"><i class="fas fa-plus-circle"></i> بيع حسابك</a>
        <a href="<%= settings.supportLink %>" class="sidebar-link"><i class="fas fa-headset"></i> الدعم الفني</a>
    </div>

    <main class="container py-5">
        <div class="row mb-5 justify-content-center">
            <div class="col-md-7">
                <div class="currency-card text-center">
                    <h6 class="fw-bold mb-3" style="color: var(--primary);">
                        <i class="fas fa-calculator me-2"></i> محول عملات WassitDZ (1$ = <%= settings.usdRate %> دج)
                    </h6>
                    <div class="input-group">
                        <input type="number" id="usdInput" class="form-control text-center" placeholder="المبلغ بالدولار $">
                        <span class="input-group-text bg-white border-0"><i class="fas fa-exchange-alt text-muted"></i></span>
                        <input type="text" id="dzpOutput" class="form-control text-center bg-light" placeholder="النتيجة بالدينار" readonly>
                    </div>
                </div>
            </div>
        </div>

        <div class="row g-3">
            <% accounts.forEach(acc => { %>
                <div class="col-6 col-md-4 col-lg-3">
                    <div class="account-card">
                        <img src="<%= acc.imgs[0] %>" class="w-100" style="height: 150px; object-fit: cover;">
                        <div class="p-3">
                            <h6 class="text-truncate fw-bold mb-1" style="font-size: 13px;"><%= acc.title %></h6>
                            <div class="mb-3"><span class="price-tag"><%= acc.priceDZ %> دج</span></div>
                            <a href="/account/<%= acc.id %>" class="btn btn-main btn-sm">التفاصيل</a>
                        </div>
                    </div>
                </div>
            <% }) %>
        </div>

        <section class="faq-section mt-5 bg-white p-4 rounded-4 border">
            <h5 class="fw-bold mb-4 text-center">الأسئلة الشائعة</h5>
            <% faqs.forEach(faq => { %>
                <div class="faq-item">
                    <div class="faq-question" onclick="this.parentElement.classList.toggle('active')">
                        <%= faq.question %> <i class="fas fa-chevron-down"></i>
                    </div>
                    <div class="faq-answer"><%= faq.answer %></div>
                </div>
            <% }) %>
        </section>

        <footer>جميع الحقوق محفوظة &copy; WassitDZ 2026</footer>
    </main>

    <script>
        function toggleMenu() {
            document.getElementById('sidebar').classList.toggle('active');
            document.getElementById('overlay').classList.toggle('active');
        }

        const rate = <%= settings.usdRate || 240 %>;
        document.getElementById('usdInput').addEventListener('input', function() {
            const val = this.value;
            document.getElementById('dzpOutput').value = val ? (val * rate).toLocaleString() + ' دج' : '';
        });
    </script>
</body>
</html>
