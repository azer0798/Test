<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>WassitDZ Game | المتجر الرسمي</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.rtl.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        :root { --primary: <%= settings.themeColor %>; }
        body { background: #f8fafc; font-family: 'Segoe UI', sans-serif; padding-top: 45px; }
        
        /* شريط الإشعار يتحرك من اليسار لليمين */
        .announcement-bar { background: #0f172a; color: #fff; height: 45px; line-height: 45px; position: fixed; top: 0; width: 100%; z-index: 2000; text-align: center; overflow: hidden; }
        
        /* تنسيق اللوجو في الوسط */
        .navbar { background: #fff; border-bottom: 2px solid var(--primary); position: sticky; top: 45px; z-index: 1000; height: 100px; }
        .navbar-brand { position: absolute; left: 50%; transform: translateX(-50%); }
        .logo-img { height: 80px; width: auto; max-width: 250px; object-fit: contain; }

        .account-card { background: #fff; border-radius: 15px; overflow: hidden; box-shadow: 0 4px 15px rgba(0,0,0,0.05); transition: 0.3s; }
        .price-dz { font-size: 1.4rem; font-weight: 800; color: #10b981; }
        
        /* تصغير زر الشراء */
        .btn-buy { background: var(--primary); color: #fff; border-radius: 8px; font-weight: 600; text-decoration: none; display: inline-block; padding: 8px 25px; font-size: 0.9rem; }
        
        .link-badge { background: #e2e8f0; color: #475569; padding: 4px 10px; border-radius: 6px; font-size: 0.75rem; font-weight: bold; }
        
        footer { background: #0f172a; color: #94a3b8; padding: 30px 0; margin-top: 50px; text-align: center; }
        .quick-links .btn { margin: 5px; border-radius: 20px; font-weight: 600; }
    </style>
</head>
<body>

<div class="announcement-bar">
    <marquee direction="right">⚡ <%= settings.announcement %> ⚡</marquee>
</div>

<nav class="navbar navbar-light bg-light">
    <div class="container d-flex justify-content-center">
        <a class="navbar-brand" href="#">
            <% if (settings.logoUrl) { %> 
                <img src="<%= settings.logoUrl %>" class="logo-img" alt="WassitDZ"> 
            <% } else { %> 
                <h2 class="fw-bold m-0" style="color: var(--primary)">WassitDZ</h2> 
            <% } %>
        </a>
    </div>
</nav>

<div class="container mt-4 text-center quick-links">
    <a href="<%= settings.mediationLink %>" class="btn btn-outline-primary"><i class="fas fa-handshake"></i> طلب وساطة</a>
    <a href="<%= settings.sellAccountLink %>" class="btn btn-outline-success"><i class="fas fa-tag"></i> بيع حسابك</a>
    <a href="<%= settings.supportLink %>" class="btn btn-outline-secondary"><i class="fas fa-headset"></i> الدعم الفني</a>
</div>

<div class="container mt-4">
    <div class="row g-4">
        <% accounts.forEach(acc => { %>
        <div class="col-md-4">
            <div class="account-card">
                <img src="<%= acc.imgs[0] %>" class="w-100" style="height: 200px; object-fit: cover;">
                <div class="p-3 text-center">
                    <div class="mb-2"><span class="link-badge"><i class="fas fa-link"></i> <%= acc.linkType %></span></div>
                    <h6 class="fw-bold mb-3"><%= acc.title %></h6>
                    <div class="price-dz mb-3"><%= acc.priceDZ %> د.ج</div>
                    <a href="<%= settings.buyNowLink %>" class="btn-buy">شراء الآن</a>
                </div>
            </div>
        </div>
        <% }) %>
    </div>

    <div class="my-5 pt-4 border-top">
        <h4 class="fw-bold mb-4 text-center">الأسئلة الشائعة (FAQ)</h4>
        <div class="accordion shadow-sm" id="faqAcc">
            <% faqs.forEach((faq, i) => { %>
            <div class="accordion-item">
                <h2 class="accordion-header"><button class="accordion-button collapsed" data-bs-toggle="collapse" data-bs-target="#f<%=i%>"><%= faq.question %></button></h2>
                <div id="f<%=i%>" class="accordion-collapse collapse" data-bs-parent="#faqAcc"><div class="accordion-body"><%= faq.answer %></div></div>
            </div>
            <% }) %>
        </div>
    </div>
</div>

<footer>
    <div class="container">
        <p class="mb-1">© <%= new Date().getFullYear() %> WassitDZ Game - متجر بيع حسابات eFootball</p>
        <p class="small">جميع الحقوق محفوظة. نضمن لك السرعة والأمان في كل معاملة.</p>
    </div>
</footer>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
