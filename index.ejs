<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>WassitDZ Game | المتجر الرسمي</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.rtl.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        :root { --primary: <%= settings.themeColor %>; --secondary: #f59e0b; }
        body { background: linear-gradient(135deg, #f8fafc 0%, #e2e8f0 100%); font-family: 'Segoe UI', sans-serif; padding-top: 45px; }
        
        .announcement-bar { background: #0f172a; color: #fff; height: 45px; line-height: 45px; position: fixed; top: 0; width: 100%; z-index: 2000; overflow: hidden; }
        .ticker { display: inline-block; animation: ticker-ltr 25s linear infinite; white-space: nowrap; }
        @keyframes ticker-ltr { 0% { transform: translateX(-100%); } 100% { transform: translateX(100%); } }

        .navbar { background: rgba(255,255,255,0.95); backdrop-filter: blur(10px); border-bottom: 2px solid var(--primary); position: sticky; top: 45px; z-index: 1000; padding: 15px 0; }
        .navbar-brand { font-weight: 900; font-size: 1.8rem; color: var(--primary) !important; margin: 0 auto; }

        .btn-service { background: #fff; padding: 10px 20px; border-radius: 12px; text-decoration: none; color: #000; font-weight: 700; transition: 0.3s; box-shadow: 0 4px 6px rgba(0,0,0,0.05); }
        .btn-service:hover { background: var(--primary); color: #fff; transform: translateY(-3px); }

        .account-card { background: #fff; border-radius: 20px; overflow: hidden; transition: 0.4s; height: 100%; border: none; box-shadow: 0 10px 20px rgba(0,0,0,0.05); }
        .account-card:hover { transform: translateY(-10px); box-shadow: 0 20px 30px rgba(0,0,0,0.1); }
        .carousel-item img { height: 240px; object-fit: cover; }

        .price-box { background: #f8fafc; padding: 15px; border-radius: 15px; border: 1px dashed #cbd5e1; margin: 15px 0; }
        .price-dz { font-size: 1.6rem; font-weight: 900; color: #10b981; }
        
        .btn-buy { background: linear-gradient(45deg, var(--primary), #4f46e5); color: #fff; padding: 14px; border-radius: 14px; font-weight: 800; text-decoration: none; display: block; text-align: center; transition: 0.3s; }
        .btn-buy:hover { background: var(--secondary); transform: scale(1.02); color: #fff; }

        footer { background: #0f172a; color: #fff; padding: 60px 0 30px; margin-top: 80px; border-top-left-radius: 50px; border-top-right-radius: 50px; text-align: center; }
    </style>
</head>
<body>

<div class="announcement-bar"><div class="ticker">⚡ <%= settings.announcement %> ⚡</div></div>

<nav class="navbar"><div class="container d-flex"><a class="navbar-brand" href="#"><i class="fas fa-gamepad"></i> WASSITDZ GAME</a></div></nav>

<div class="container mt-4">
    <div class="d-flex justify-content-center gap-2 mb-5 flex-wrap">
        <a href="<%= settings.supportLink %>" class="btn-service">الدعم الفني</a>
        <a href="<%= settings.mediationLink %>" class="btn-service">الوساطة</a>
        <a href="<%= settings.sellAccountLink %>" class="btn-service">بيع حسابك</a>
    </div>

    <div class="row g-4" id="accountsGrid">
        <% accounts.forEach(acc => { %>
        <div class="col-xl-4 col-md-6 account-item" data-price="<%= acc.priceDZ %>">
            <div class="account-card">
                <div id="c-<%= acc.id %>" class="carousel slide" data-bs-ride="false">
                    <div class="carousel-inner">
                        <% acc.imgs.forEach((img, i) => { %>
                        <div class="carousel-item <%= i==0?'active':'' %>"><img src="<%= img %>" class="d-block w-100"></div>
                        <% }) %>
                    </div>
                    <button class="carousel-control-prev" type="button" data-bs-target="#c-<%= acc.id %>" data-bs-slide="prev"><span class="carousel-control-prev-icon"></span></button>
                    <button class="carousel-control-next" type="button" data-bs-target="#c-<%= acc.id %>" data-bs-slide="next"><span class="carousel-control-next-icon"></span></button>
                </div>
                <div class="card-body p-4">
                    <div class="d-flex justify-content-between mb-2"><span class="badge bg-dark">#<%= acc.id %></span><span class="small fw-bold text-primary"><%= acc.linkType %></span></div>
                    <h5 class="fw-bold mb-3"><%= acc.title %></h5>
                    <div class="price-box text-center">
                        <div class="price-dz"><%= acc.priceDZ %> د.ج</div>
                        <div class="text-muted small fw-bold">$<%= acc.priceUSD %> USDT</div>
                    </div>
                    <a href="<%= settings.buyNowLink %>?text=<%= encodeURIComponent('شراء حساب #' + acc.id) %>" class="btn-buy">شراء الحساب الآن</a>
                </div>
            </div>
        </div>
        <% }) %>
    </div>
</div>

<footer>
    <div class="footer-logo"><i class="fas fa-gamepad"></i> WASSITDZ GAME</div>
    <p class="text-muted small">جميع الحقوق محفوظة © 2026</p>
</footer>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
