<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>WassitDZ Game | متجر الحسابات</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.rtl.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        :root { 
            --primary: <%= settings.themeColor || '#007bff' %>; 
            --bg-body: #f4f6f8;
        }
        
        body { background: var(--bg-body); font-family: 'Segoe UI', sans-serif; padding-top: 40px; margin: 0; overflow-x: hidden; }

        /* شريط الإعلان */
        .top-notice { background: #000; color: #fff; height: 40px; line-height: 40px; position: fixed; top: 0; width: 100%; z-index: 2000; font-size: 13px; font-weight: bold; text-align: center; }

        /* الهيدر: القائمة يمين واللوغو يسار */
        .header-main { 
            background: #fff; padding: 15px 20px; border-bottom: 1px solid #eee;
            display: flex; align-items: center; justify-content: space-between;
            position: sticky; top: 40px; z-index: 1000; flex-direction: row-reverse;
        }
        .header-main img { max-height: 60px; width: auto; }
        .menu-btn { background: #fff; border: 1px solid #eee; padding: 10px 15px; border-radius: 8px; cursor: pointer; color: #333; font-size: 20px; }

        /* القائمة الجانبية (Sidebar) */
        .sidebar { position: fixed; top: 0; right: -280px; width: 280px; height: 100%; background: #fff; z-index: 3000; transition: 0.4s ease; box-shadow: -5px 0 15px rgba(0,0,0,0.1); padding: 20px; }
        .sidebar.active { right: 0; }
        .sidebar-overlay { position: fixed; top: 0; left: 0; width: 100%; height: 100%; background: rgba(0,0,0,0.5); z-index: 2999; display: none; }
        .sidebar-overlay.active { display: block; }
        .sidebar-header { display: flex; justify-content: space-between; align-items: center; margin-bottom: 30px; border-bottom: 1px solid #eee; padding-bottom: 15px; }
        .sidebar-link { display: block; padding: 15px; color: #333; text-decoration: none; font-weight: bold; border-radius: 10px; margin-bottom: 10px; transition: 0.3s; }
        .sidebar-link:hover { background: #f8f9fa; color: var(--primary); }
