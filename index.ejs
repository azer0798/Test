<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>WassitDZ Game | المتجر الرسمي</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.rtl.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        :root { --primary: <%= settings.themeColor %>; }
        body { background: #f8fafc; font-family: 'Segoe UI', sans-serif; padding-top: 45px; }
        .announcement-bar { background: #0f172a; color: #fff; height: 45px; line-height: 45px; position: fixed; top: 0; width: 100%; z-index: 2000; overflow: hidden; }
        
        /* اللوجو في الوسط تماماً وكبير */
        .navbar { background: #fff; border-bottom: 3px solid var(--primary); height: 140px; position: sticky; top: 45px; z-index: 1000; display: flex; align-items: center; justify-content: center; }
        .logo-img { max-height: 110px; width: auto; max-width: 90%; object-fit: contain; }

        /* ميزة ملء الإطار للصور */
        .img-container { width: 100%; height: 210px; overflow: hidden; background: #eee; }
        .img-container img { width: 100%; height: 100%; object-fit: cover; }

        .account-card { background: #fff; border-radius: 12px; overflow: hidden; box-shadow: 0 4px 10px rgba(0,0,0,0.05); transition: 0.3s; height: 100%; border: 1px solid #eef2f6; }
        .price-dz { font-size: 1.25rem; font-weight: 800; color: #10b981; }
        .price-usd { font-size: 0.95rem; color: #64748b; font-weight: 600; margin-bottom: 8px; }
        .btn-buy { background: var(--primary); color: #fff; border-radius: 6px; padding: 5px 20px; font-size: 0.85rem; text-decoration: none; font-weight: bold; }
        .link-badge { background: #f1f5f9; color: #475569; padding: 3px 10px; border-radius: 5px; font-size: 0.7rem; font-weight: bold; }
        footer { background: #0f172a; color: #94a3b8; padding: 30px 0; margin-top: 50px; text-align: center; }
    </style>
</head>
<body>

<div class="announcement-bar">
    <marquee direction="right">⚡ <%= settings.announcement %> ⚡</marquee>
</div>

<nav class="navbar navbar-light">
    <a href="/" class="navbar-brand m-0">
        <% if (settings.logoUrl) { %> <img src="<%= settings.logoUrl %>" class="logo-img"> <% } else { %> <h2 class="fw-bold m-0" style="color: var(--primary)">WassitDZ</h2> <% } %>
    </a>
</nav>

<div class="container mt-3 text-center">
    <a href="<%= settings.mediationLink %>" class="btn btn-sm btn-outline-primary m-1 px-3">الوساطة</a>
    <a href="<%= settings.sellAccountLink %>" class="btn btn-sm btn-outline-success m-1 px-3">بيع حسابك</a>
    <a href="<%= settings.supportLink %>" class="btn btn-sm btn-outline-secondary m-1 px-3">الدعم</a>
</div>

<div class="container mt-4">
    <div class="row g-3">
        <% accounts.forEach(acc => { %>
        <div class="col-6 col-md-4 col-lg-3">
            <div class="account-card">
                <div class="img-container"><img src="<%= acc.imgs[0] %>"></div>
                <div class="p-3 text-center">
                    <span class="link-badge"><%= acc.linkType %></span>
                    <h6 class="fw-bold my-2 small text-truncate"><%= acc.title %></h6>
                    <div class="price-dz"><%= acc.priceDZ %> د.ج</div>
                    <div class="price-usd"><%= acc.priceUSD %> $</div>
                    <a href="<%= settings.buyNowLink %>" class="btn-buy shadow-sm">شراء</a>
                </div>
            </div>
        </div>
        <% }) %>
    </div>

    <div class="my-5 border-top pt-5">
        <h5 class="fw-bold mb-4 text-center">الأسئلة الشائعة</h5>
        <div class="accordion accordion-flush" id="faqAcc">
            <% faqs.forEach((faq, i) => { %>
            <div class="accordion-item mb-2 border rounded">
                <h2 class="accordion-header"><button class="accordion-button collapsed py-2 small fw-bold" data-bs-toggle="collapse" data-bs-target="#f<%=i%>"><%= faq.question %></button></h2>
                <div id="f<%=i%>" class="accordion-collapse collapse" data-bs-parent="#faqAcc"><div class="accordion-body small text-muted"><%= faq.answer %></div></div>
            </div>
            <% }) %>
        </div>
    </div>
</div>

<footer>
    <div class="container">
        <p class="mb-1">© <%= new Date().getFullYear() %> WassitDZ Game</p>
        <p class="small opacity-50">جميع الحقوق محفوظة • أمان • سرعة • موثوقية</p>
    </div>
</footer>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
