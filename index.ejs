<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>WassitDZ Game | متجر الحسابات</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.rtl.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Cairo:wght@400;600;700&display=swap" rel="stylesheet">
    
    <style>
        :root { 
            --primary: #ff4d94; /* وردي ملكي */
            --bg-body: #fff5f8; /* خلفية وردية خفيفة جداً */
            --card-bg: #ffffff;
            --text-dark: #444;
        }
        
        body { 
            background: var(--bg-body); 
            color: var(--text-dark);
            font-family: 'Cairo', sans-serif; 
            padding-top: 40px; 
            margin: 0; 
        }

        /* شريط الإعلان */
        .top-notice { 
            background: var(--primary); 
            color: #fff; 
            height: 40px; 
            line-height: 40px; 
            position: fixed; 
            top: 0; width: 100%; 
            z-index: 2000; 
            font-size: 13px; 
            font-weight: bold; 
            text-align: center;
        }

        /* الهيدر */
        .header-main { 
            background: rgba(255, 255, 255, 0.9);
            backdrop-filter: blur(10px);
            padding: 10px 20px; 
            border-bottom: 2px solid #ffe6f0;
            display: flex; align-items: center; justify-content: space-between;
            position: sticky; top: 40px; z-index: 1000; flex-direction: row-reverse;
        }
        .header-main img { max-height: 55px; }
        
        .menu-btn { 
            background: #fff; 
            border: 2px solid #ffe6f0; 
            padding: 8px 12px; 
            border-radius: 12px; 
            cursor: pointer; 
            color: var(--primary); 
            font-size: 18px;
            transition: 0.3s;
        }
        .menu-btn:hover { background: var(--primary); color: #fff; }

        /* القائمة الجانبية */
        .sidebar { 
            position: fixed; top: 0; right: -280px; width: 280px; height: 100%; 
            background: #fff; z-index: 3000; transition: 0.4s ease; 
            box-shadow: -5px 0 20px rgba(255, 77, 148, 0.1); padding: 25px;
        }
        .sidebar.active { right: 0; }
        .sidebar-overlay { position: fixed; top: 0; left: 0; width: 100%; height: 100%; background: rgba(0,0,0,0.3); z-index: 2999; display: none; }
        .sidebar-overlay.active { display: block; }
        
        .sidebar-link { 
            display: flex; align-items: center; gap: 12px; padding: 12px; 
            color: #555; text-decoration: none; font-weight: 600; border-radius: 10px; 
            margin-bottom: 8px; transition: 0.3s; font-size: 14px;
        }
        .sidebar-link i { 
            font-size: 16px; width: 32px; height: 32px; background: #fff0f5;
            display: flex; align-items: center; justify-content: center; border-radius: 8px;
            color: var(--primary);
        }
        .sidebar-link:hover { background: #fff0f5; color: var(--primary); }

        /* بطاقة الحساب */
        .account-card {
            background: var(--card-bg);
            border-radius: 18px;
            border: 2px solid #ffeef4;
            overflow: hidden;
            transition: 0.3s;
            margin-bottom: 20px;
        }
        .account-card:hover {
            transform: translateY(-5px);
            border-color: var(--primary);
            box-shadow: 0 10px 20px rgba(255, 77, 148, 0.1);
        }
        
        .price-tag { color: var(--primary); font-weight: 800; font-size: 1.1rem; }

        .btn-main {
            background: var(--primary);
            border: none; border-radius: 10px; color: #fff; font-weight: 700;
            padding: 8px; width: 100%; transition: 0.3s;
        }
        .btn-main:hover { opacity: 0.9; box-shadow: 0 4px 12px rgba(255, 77, 148, 0.3); }

        /* قسم الأسئلة الشائعة */
        .faq-section { background: #fff; border-radius: 20px; padding: 25px; margin-top: 40px; border: 1px solid #ffeef4; }
        .faq-item { border-bottom: 1px solid #f9f9f9; padding: 15px 0; }
        .faq-question { font-weight: 700; color: var(--primary); margin-bottom: 5px; cursor: pointer; display: flex; align-items: center; gap: 10px; }
        .faq-answer { font-size: 14px; color: #777; padding-right: 25px; }

        /* الحقوق */
        footer { text-align: center; padding: 30px; color: #aaa; font-size: 13px; }
    </style>
</head>
<body>

    <div class="top-notice">
        <i class="fas fa-star me-2"></i> <%= settings.announcement %>
    </div>

    <header class="header-main">
        <div class="logo">
            <img src="<%= settings.logoUrl %>" alt="WassitDZ">
        </div>
        <button class="menu-btn" onclick="toggleMenu()">
            <i class="fas fa-bars-staggered"></i>
        </button>
    </header>

    <div class="sidebar-overlay" id="overlay" onclick="toggleMenu()"></div>
    
    <div class="sidebar" id="sidebar">
        <div class="sidebar-header d-flex justify-content-between align-items-center mb-4 border-bottom pb-3">
            <h5 class="m-0 fw-bold" style="color:var(--primary)">التنقل</h5>
            <button class="btn-close" onclick="toggleMenu()"></button>
        </div>
        <a href="/" class="sidebar-link"><i class="fas fa-home"></i> الرئيسية</a>
        <a href="<%= settings.mediationLink %>" class="sidebar-link"><i class="fas fa-handshake"></i> طلب وساطة</a>
        <a href="<%= settings.sellAccountLink %>" class="sidebar-link"><i class="fas fa-plus-circle"></i> بيع حسابك</a>
        <a href="<%= settings.supportLink %>" class="sidebar-link"><i class="fas fa-comment-dots"></i> الدعم الفني</a>
    </div>

    <main class="container py-5">
        <div class="row g-3">
            <% accounts.forEach(acc => { %>
                <div class="col-6 col-md-4 col-lg-3">
                    <div class="account-card">
                        <img src="<%= acc.imgs[0] %>" class="w-100" style="height: 150px; object-fit: cover;">
                        <div class="p-3">
                            <h6 class="text-truncate fw-bold mb-1" style="font-size: 13px;"><%= acc.title %></h6>
                            <div class="mb-3"><span class="price-tag"><%= acc.priceDZ %> دج</span></div>
                            <a href="/account/<%= acc.id %>" class="btn btn-main btn-sm">عرض التفاصيل</a>
                        </div>
                    </div>
                </div>
            <% }) %>
        </div>

        <section class="faq-section">
            <h5 class="fw-bold mb-4 text-center"><i class="fas fa-question-circle me-2"></i> الأسئلة الشائعة</h5>
            <% faqs.forEach(faq => { %>
                <div class="faq-item">
                    <div class="faq-question"><i class="fas fa-chevron-left fa-xs"></i> <%= faq.question %></div>
                    <div class="faq-answer"><%= faq.answer %></div>
                </div>
            <% }) %>
        </section>

        <footer>
            <p>جميع الحقوق محفوظة &copy; WassitDZ 2026</p>
            <div style="font-size: 10px; opacity: 0.5;">تم التطوير بكل ❤️ لخدمة الجيمرز في الجزائر</div>
        </footer>
    </main>

    <script>
        function toggleMenu() {
            document.getElementById('sidebar').classList.toggle('active');
            document.getElementById('overlay').classList.toggle('active');
        }
    </script>
</body>
</html>
