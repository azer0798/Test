<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>WassitDZ Game | المتجر الرسمي</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.rtl.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        :root { --primary: <%= settings.themeColor %>; }
        body { background: #f8fafc; font-family: 'Segoe UI', sans-serif; padding-top: 45px; }
        .announcement-bar { background: #0f172a; color: #fff; height: 45px; line-height: 45px; position: fixed; top: 0; width: 100%; z-index: 2000; text-align: center; }
        .navbar { background: #fff; border-bottom: 2px solid var(--primary); position: sticky; top: 45px; z-index: 1000; }
        .account-card { background: #fff; border-radius: 15px; overflow: hidden; box-shadow: 0 4px 15px rgba(0,0,0,0.05); transition: 0.3s; }
        .account-card:hover { transform: translateY(-5px); }
        .price-dz { font-size: 1.5rem; font-weight: 800; color: #10b981; }
        .btn-buy { background: var(--primary); color: #fff; border-radius: 10px; font-weight: 700; text-decoration: none; display: block; text-align: center; padding: 12px; }
    </style>
</head>
<body>

<div class="announcement-bar"><marquee>⚡ <%= settings.announcement %> ⚡</marquee></div>

<nav class="navbar navbar-light bg-light">
    <div class="container">
        <a class="navbar-brand fw-bold" href="#">
            <% if (settings.logoUrl) { %> <img src="<%= settings.logoUrl %>" height="40"> <% } else { %> WassitDZ <% } %>
        </a>
    </div>
</nav>

<div class="container mt-4">
    <div class="row g-4">
        <% accounts.forEach(acc => { %>
        <div class="col-md-4">
            <div class="account-card">
                <img src="<%= acc.imgs[0] %>" class="w-100" style="height: 200px; object-fit: cover;">
                <div class="p-3">
                    <h6 class="fw-bold"><%= acc.title %></h6>
                    <div class="price-dz mb-3 text-center"><%= acc.priceDZ %> د.ج</div>
                    <a href="<%= settings.buyNowLink %>" class="btn-buy">شراء الآن</a>
                </div>
            </div>
        </div>
        <% }) %>
    </div>

    <div class="my-5">
        <h4 class="fw-bold mb-4 text-center">الأسئلة الشائعة</h4>
        <div class="accordion" id="faqAcc">
            <% faqs.forEach((faq, i) => { %>
            <div class="accordion-item">
                <h2 class="accordion-header"><button class="accordion-button collapsed" data-bs-toggle="collapse" data-bs-target="#f<%=i%>"><%= faq.question %></button></h2>
                <div id="f<%=i%>" class="accordion-collapse collapse" data-bs-parent="#faqAcc"><div class="accordion-body"><%= faq.answer %></div></div>
            </div>
            <% }) %>
        </div>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
