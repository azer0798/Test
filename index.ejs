<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>WassitDZ Game | المتجر</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.rtl.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        :root { --primary: #ff4d94; --bg: #fff5f8; }
        body { background: var(--bg); font-family: sans-serif; padding-top: 40px; }
        .top-notice { background: var(--primary); color: #fff; height: 40px; position: fixed; top: 0; width: 100%; z-index: 2000; overflow: hidden; display: flex; align-items: center; }
        .scrolling-text { display: inline-block; white-space: nowrap; animation: continuous-scroll 20s linear infinite; font-size: 13px; font-weight: bold; }
        @keyframes continuous-scroll { 0% { transform: translateX(100%); } 100% { transform: translateX(-100%); } }
        .account-card { background: #fff; border-radius: 15px; overflow: hidden; border: 1px solid #ffeef4; transition: 0.3s; }
        .account-card:hover { transform: translateY(-5px); border-color: var(--primary); }
        .currency-card { background: #fff; border-radius: 15px; border: 2px dashed #ffe6f0; padding: 15px; margin-bottom: 30px; }
    </style>
</head>
<body>

    <div class="top-notice">
        <div class="scrolling-text">
            <i class="fas fa-star me-2"></i> <%= settings.announcement %> &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp; <i class="fas fa-star me-2"></i> <%= settings.announcement %>
        </div>
    </div>

    <main class="container py-5">
        <div class="row mb-4 justify-content-center">
            <div class="col-md-6">
                <div class="currency-card text-center">
                    <h6 class="fw-bold mb-2" style="color: var(--primary);">محول العملات (1$ = <%= settings.usdRate %> دج)</h6>
                    <div class="input-group input-group-sm">
                        <input type="number" id="usdInput" class="form-control text-center" placeholder="بالدولار $">
                        <input type="text" id="dzpOutput" class="form-control text-center bg-light" placeholder="بالدينار" readonly>
                    </div>
                </div>
            </div>
        </div>

        <div class="row g-3">
            <% accounts.forEach(acc => { %>
                <div class="col-6 col-md-4 col-lg-3">
                    <div class="account-card position-relative shadow-sm">
                        <span class="badge position-absolute top-0 start-0 m-2 <%= acc.status === 'متاح' ? 'bg-success' : 'bg-danger' %>" style="z-index: 5; font-size: 10px;">
                            <%= acc.status %>
                        </span>

                        <img src="<%= acc.imgs[0] %>" class="w-100" style="height: 140px; object-fit: cover;">
                        
                        <div class="p-3 text-center">
                            <h6 class="text-truncate fw-bold mb-1" style="font-size: 13px;"><%= acc.title %></h6>
                            
                            <div class="small text-muted mb-2" style="font-size: 10px;">
                                <i class="fas fa-coins text-warning"></i> <%= acc.coins %> | <i class="fas fa-gem text-info"></i> <%= acc.gems %>
                            </div>

                            <div class="mb-3"><span class="fw-bold" style="color: var(--primary);"><%= acc.priceDZ %> دج</span></div>
                            
                            <div class="d-grid gap-2">
                                <a href="https://wa.me/<%= settings.supportLink %>?text=السلام+عليكم+WassitDZ،+أريد+شراء+الحساب+رقم:+%23<%= acc.id %>+<%= acc.title %>" 
                                   class="btn btn-sm text-white fw-bold" style="background: var(--primary); border-radius: 8px;">
                                   شراء الآن
                                </a>
                                <a href="/account/<%= acc.id %>" class="text-muted small text-decoration-none" style="font-size: 10px;">التفاصيل</a>
                            </div>
                        </div>
                    </div>
                </div>
            <% }) %>
        </div>

        <section class="mt-5 p-4 bg-white rounded-4 border">
            <h5 class="fw-bold mb-3 text-center">الأسئلة الشائعة</h5>
            <% faqs.forEach(faq => { %>
                <div class="border-bottom py-2">
                    <div class="fw-bold text-primary" style="cursor:pointer" onclick="this.nextElementSibling.classList.toggle('d-none')">
                        <%= faq.question %>
                    </div>
                    <div class="text-muted small d-none mt-1"><%= faq.answer %></div>
                </div>
            <% }) %>
        </section>
    </main>

    <script>
        const rate = <%= settings.usdRate || 240 %>;
        document.getElementById('usdInput').addEventListener('input', function() {
            document.getElementById('dzpOutput').value = this.value ? (this.value * rate).toLocaleString() + ' دج' : '';
        });
    </script>
</body>
</html>
