<!DOCTYPE html>
<form action="/auth-admin" method="POST">
    <meta charset="UTF-8">
    <title>تسجيل الدخول | WassitDZ</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.rtl.min.css" rel="stylesheet">
    <style>
        body { background: #0f172a; height: 100vh; display: flex; align-items: center; justify-content: center; }
        .login-card { background: #fff; padding: 40px; border-radius: 20px; width: 100%; max-width: 400px; box-shadow: 0 10px 25px rgba(0,0,0,0.5); }
    </style>
</head>
<body>
    <div class="login-card text-center">
        <h4 class="fw-bold mb-4">لوحة تحكم المدير</h4>
        <form action="/login-auth" method="POST">
            <input type="text" name="username" class="form-control mb-3" placeholder="اسم المستخدم" required>
            <input type="password" name="password" class="form-control mb-4" placeholder="كلمة السر" required>
            <button class="btn btn-primary w-100 fw-bold">دخول</button>
        </form>
    </div>
</body>
</html>
