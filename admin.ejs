<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>لوحة الإدارة | WassitDZ</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.rtl.min.css" rel="stylesheet">
    <style>body { background: #f1f5f9; padding: 20px; }</style>
</head>
<body>
<div class="container">
    <div class="card p-4 shadow-sm border-0 mb-4">
        <h5 class="fw-bold mb-3">إعدادات المتجر وروابط الدعم</h5>
        <form action="/update-settings" method="POST" class="row g-3">
            <div class="col-md-9"><label class="small fw-bold">نص الإعلان العلوي</label><input type="text" name="announcement" class="form-control" value="<%= settings.announcement %>"></div>
            <div class="col-md-3"><label class="small fw-bold">لون التنسيق</label><input type="color" name="themeColor" class="form-control form-control-color w-100" value="<%= settings.themeColor %>"></div>
            <div class="col-md-3"><label class="small fw-bold">رابط الدعم</label><input type="text" name="supportLink" class="form-control" value="<%= settings.supportLink %>"></div>
            <div class="col-md-3"><label class="small fw-bold">رابط الوساطة</label><input type="text" name="mediationLink" class="form-control" value="<%= settings.mediationLink %>"></div>
            <div class="col-md-3"><label class="small fw-bold">رابط بيع حساب</label><input type="text" name="sellAccountLink" class="form-control" value="<%= settings.sellAccountLink %>"></div>
            <div class="col-md-3"><label class="small fw-bold">رابط الشراء (WhatsApp/Telegram)</label><input type="text" name="buyNowLink" class="form-control" value="<%= settings.buyNowLink %>"></div>
            <div class="col-12 text-end"><button type="submit" class="btn btn-dark px-4">حفظ كافة الإعدادات</button></div>
        </form>
    </div>

    <div class="row">
        <div class="col-md-5">
            <div class="card p-4 shadow-sm border-0">
                <h6 class="fw-bold mb-3">نشر حساب جديد</h6>
                <form action="/add-account" method="POST" enctype="multipart/form-data">
                    <input type="text" name="title" class="form-control mb-2" placeholder="عنوان الحساب (مثلاً: حساب ليفل 100)" required>
                    <div class="row g-2 mb-2">
                        <div class="col-6"><input type="text" name="priceUSD" class="form-control" placeholder="السعر بالـ USDT" required></div>
                        <div class="col-6"><input type="text" name="priceDZ" class="form-control" placeholder="السعر بالدينار" required></div>
                    </div>
                    <select name="linkType" class="form-select mb-2">
                        <option>ID Konami (مربوط)</option>
                        <option>Google Play</option>
                        <option>غير مربوط (صافي)</option>
                    </select>
                    <label class="small mb-1 d-block">اختر الصور (حتى 5 صور):</label>
                    <input type="file" name="imageFiles" class="form-control mb-3" multiple required>
                    <button type="submit" class="btn btn-primary w-100 fw-bold">نشر الحساب الآن</button>
                </form>
            </div>
        </div>
        <div class="col-md-7">
            <div class="card p-4 shadow-sm border-0 mt-3 mt-md-0">
                <h6 class="fw-bold mb-3">الحسابات المعروضة حالياً</h6>
                <div class="table-responsive">
                    <table class="table align-middle">
                        <thead><tr><th>ID</th><th>العنوان</th><th>السعر</th><th>إجراء</th></tr></thead>
                        <tbody>
                            <% accounts.forEach(acc => { %>
                            <tr>
                                <td>#<%= acc.id %></td>
                                <td class="small"><%= acc.title %></td>
                                <td><span class="badge bg-success"><%= acc.priceDZ %> د.ج</span></td>
                                <td><a href="/delete/<%= acc.id %>" class="btn btn-outline-danger btn-sm" onclick="return confirm('هل أنت متأكد؟')">حذف</a></td>
                            </tr>
                            <% }) %>
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>
</body>
</html>
