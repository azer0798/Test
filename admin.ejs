<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <title>لوحة الإدارة | WassitDZ</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.rtl.min.css" rel="stylesheet">
    <style>body { background: #f1f5f9; padding: 40px 0; }</style>
</head>
<body>
<div class="container">
    <div class="card p-4 shadow-sm border-0 mb-4">
        <h5 class="fw-bold mb-3">إعدادات الموقع والروابط</h5>
        <form action="/update-settings" method="POST" class="row g-3">
            <div class="col-md-9"><label class="small fw-bold">نص الشريط العلوي</label><input type="text" name="announcement" class="form-control" value="<%= settings.announcement %>"></div>
            <div class="col-md-3"><label class="small fw-bold">لون الموقع</label><input type="color" name="themeColor" class="form-control form-control-color w-100" value="<%= settings.themeColor %>"></div>
            <div class="col-md-3"><label class="small fw-bold">رابط الدعم</label><input type="text" name="supportLink" class="form-control" value="<%= settings.supportLink %>"></div>
            <div class="col-md-3"><label class="small fw-bold">رابط الوساطة</label><input type="text" name="mediationLink" class="form-control" value="<%= settings.mediationLink %>"></div>
            <div class="col-md-3"><label class="small fw-bold">رابط بيع حسابك</label><input type="text" name="sellAccountLink" class="form-control" value="<%= settings.sellAccountLink %>"></div>
            <div class="col-md-3"><label class="small fw-bold">رابط شراء الآن</label><input type="text" name="buyNowLink" class="form-control" value="<%= settings.buyNowLink %>"></div>
            <div class="col-12 text-end"><button class="btn btn-dark">حفظ الإعدادات</button></div>
        </form>
    </div>

    <div class="row">
        <div class="col-md-5">
            <div class="card p-4 shadow-sm border-0">
                <h6 class="fw-bold mb-3">إضافة حساب جديد</h6>
                <form action="/add-account" method="POST" enctype="multipart/form-data">
                    <input type="text" name="title" class="form-control mb-2" placeholder="العنوان" required>
                    <div class="row g-2 mb-2">
                        <div class="col-6"><input type="text" name="priceUSD" class="form-control" placeholder="السعر $" required></div>
                        <div class="col-6"><input type="text" name="priceDZ" class="form-control" placeholder="السعر د.ج" required></div>
                    </div>
                    <select name="linkType" class="form-select mb-2">
                        <option>ID Konami</option><option>Google Play</option><option>غير مربوط</option>
                    </select>
                    <input type="file" name="imageFiles" class="form-control mb-3" multiple required>
                    <button class="btn btn-primary w-100">نشر الحساب</button>
                </form>
            </div>
        </div>
        <div class="col-md-7">
            <div class="card p-4 shadow-sm border-0">
                <table class="table align-middle">
                    <thead><tr><th>ID</th><th>السعر</th><th>-</th></tr></thead>
                    <tbody>
                        <% accounts.forEach(acc => { %>
                        <tr><td>#<%= acc.id %></td><td><%= acc.priceDZ %> د.ج</td><td><a href="/delete/<%= acc.id %>" class="btn btn-danger btn-sm">حذف</a></td></tr>
                        <% }) %>
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>
</body>
</html>
