<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <title>لوحة التحكم | WassitDZ</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.rtl.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        body { background: #f4f6f9; font-family: sans-serif; }
        .card { border-radius: 15px; border: none; box-shadow: 0 5px 15px rgba(0,0,0,0.05); margin-bottom: 20px; }
        .nav-tabs .nav-link { color: #555; font-weight: bold; }
        .nav-tabs .nav-link.active { color: #ff4d94; border-bottom: 3px solid #ff4d94; }
    </style>
</head>
<body class="p-4">

    <div class="container">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h2 class="fw-bold">لوحة تحكم WassitDZ</h2>
            <a href="/" class="btn btn-outline-secondary btn-sm">عرض الموقع</a>
        </div>

        <ul class="nav nav-tabs mb-4" id="adminTabs">
            <li class="nav-item"><a class="nav-link active" data-bs-toggle="tab" href="#accounts">الحسابات</a></li>
            <li class="nav-item"><a class="nav-link" data-bs-toggle="tab" href="#settings">إعدادات الموقع والعملة</a></li>
            <li class="nav-item"><a class="nav-link" data-bs-toggle="tab" href="#faqs">الأسئلة الشائعة</a></li>
        </ul>

        <div class="tab-content">
            <div class="tab-pane fade show active" id="accounts">
                <div class="card p-4">
                    <h5 class="mb-3">إضافة حساب جديد</h5>
                    <form action="/add-account" method="POST" enctype="multipart/form-data">
                        <div class="row">
                            <div class="col-md-6 mb-3"><input type="text" name="title" class="form-control" placeholder="عنوان الحساب" required></div>
                            <div class="col-md-3 mb-3"><input type="text" name="priceDZ" class="form-control" placeholder="السعر بالدينار"></div>
                            <div class="col-md-3 mb-3"><input type="text" name="priceUSD" class="form-control" placeholder="السعر بالدولار"></div>
                            <div class="col-md-12 mb-3"><input type="file" name="imageFiles" class="form-control" multiple required></div>
                        </div>
                        <button class="btn btn-primary w-100">نشر الحساب</button>
                    </form>
                </div>

                <div class="row">
                    <% accounts.forEach(acc => { %>
                    <div class="col-md-4">
                        <div class="card p-2">
                            <img src="<%= acc.imgs[0] %>" style="height:120px; object-fit:cover; border-radius:10px;">
                            <div class="p-2">
                                <h6 class="text-truncate"><%= acc.title %></h6>
                                <div class="d-flex justify-content-between">
                                    <a href="/toggle-status/<%= acc.id %>" class="btn btn-sm <%= acc.status === 'متاح' ? 'btn-success' : 'btn-warning' %>"><%= acc.status %></a>
                                    <a href="/delete-account/<%= acc.id %>" class="btn btn-danger btn-sm" onclick="return confirm('حذف؟')">حذف</a>
                                </div>
                            </div>
                        </div>
                    </div>
                    <% }) %>
                </div>
            </div>

            <div class="tab-pane fade" id="settings">
                <div class="card p-4">
                    <h5 class="mb-4 text-primary"><i class="fas fa-coins me-2"></i> تحديث سعر الصرف والإعدادات</h5>
                    <form action="/update-settings" method="POST">
                        <div class="row bg-light p-3 rounded-4 mb-4">
                            <div class="col-md-6">
                                <label class="fw-bold mb-2">سعر صرف 1 دولار بالدينار (السكوار):</label>
                                <input type="number" name="usdRate" class="form-control form-control-lg text-center" value="<%= settings.usdRate %>" style="border: 2px solid #ff4d94; color: #ff4d94; font-weight: bold;">
                            </div>
                            <div class="col-md-6">
                                <label class="fw-bold mb-2">رابط اللوغو (URL):</label>
                                <input type="text" name="logoUrl" class="form-control" value="<%= settings.logoUrl %>">
                            </div>
                        </div>
                        <div class="row">
                            <div class="col-md-6 mb-3"><label>رابط الوساطة:</label><input type="text" name="mediationLink" class="form-control" value="<%= settings.mediationLink %>"></div>
                            <div class="col-md-6 mb-3"><label>رابط بيع حسابك:</label><input type="text" name="sellAccountLink" class="form-control" value="<%= settings.sellAccountLink %>"></div>
                            <div class="col-md-12 mb-3"><label>شريط الإعلان:</label><input type="text" name="announcement" class="form-control" value="<%= settings.announcement %>"></div>
                        </div>
                        <button class="btn btn-success w-100 py-2 fw-bold">حفظ التغييرات</button>
                    </form>
                </div>
            </div>

            <div class="tab-pane fade" id="faqs">
                <div class="card p-4">
                    <h5>إضافة سؤال وجواب</h5>
                    <form action="/add-faq" method="POST" class="mb-4">
                        <input type="text" name="question" class="form-control mb-2" placeholder="السؤال" required>
                        <textarea name="answer" class="form-control mb-2" placeholder="الجواب" required></textarea>
                        <button class="btn btn-dark w-100">إضافة</button>
                    </form>
                    <table class="table">
                        <% faqs.forEach(f => { %>
                        <tr>
                            <td><%= f.question %></td>
                            <td><a href="/delete-faq/<%= f._id %>" class="btn btn-danger btn-sm">حذف</a></td>
                        </tr>
                        <% }) %>
                    </table>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
