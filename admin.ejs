<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <title>لوحة التحكم</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.rtl.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>body { background: #f1f5f9; padding: 20px; font-family: 'Segoe UI', sans-serif; } .card { border-radius: 15px; border: none; box-shadow: 0 4px 10px rgba(0,0,0,0.05); margin-bottom: 20px; }</style>
</head>
<body>
    <div class="container">
        <div class="card p-4">
            <h5 class="fw-bold mb-3 border-bottom pb-2">إعدادات الموقع</h5>
            <form action="/update-settings" method="POST" class="row g-3">
                <div class="col-md-4"><label>رابط اللوجو</label><input type="text" name="logoUrl" class="form-control" value="<%= settings.logoUrl %>"></div>
                <div class="col-md-2"><label>اللون</label><input type="color" name="themeColor" class="form-control" value="<%= settings.themeColor %>"></div>
                <div class="col-md-6"><label>الإشهار</label><input type="text" name="announcement" class="form-control" value="<%= settings.announcement %>"></div>
                <div class="col-md-3"><label>رابط الشراء</label><input type="text" name="buyNowLink" class="form-control" value="<%= settings.buyNowLink %>"></div>
                <div class="col-md-3"><label>رابط الوساطة</label><input type="text" name="mediationLink" class="form-control" value="<%= settings.mediationLink %>"></div>
                <div class="col-md-3"><label>رابط البيع</label><input type="text" name="sellAccountLink" class="form-control" value="<%= settings.sellAccountLink %>"></div>
                <div class="col-md-3"><label>رابط الدعم</label><input type="text" name="supportLink" class="form-control" value="<%= settings.supportLink %>"></div>
                <div class="col-12 text-end"><button class="btn btn-dark px-5">حفظ الإعدادات</button></div>
            </form>
        </div>

        <div class="row">
            <div class="col-md-5">
                <div class="card p-4">
                    <h6 class="fw-bold mb-3">نشر حساب جديد</h6>
                    <form action="/add-account" method="POST" enctype="multipart/form-data">
                        <input type="text" name="title" class="form-control mb-2" placeholder="العنوان" required>
                        <div class="row g-2 mb-2">
                            <div class="col-6"><input type="text" name="priceDZ" class="form-control" placeholder="السعر د.ج" required></div>
                            <div class="col-6"><input type="text" name="priceUSD" class="form-control" placeholder="السعر $" required></div>
                        </div>
                        <select name="linkType" class="form-select mb-2">
                            <option value="Id Konami">Id Konami</option>
                            <option value="Google Play">Google Play</option>
                        </select>
                        <input type="file" name="imageFiles" class="form-control mb-3" multiple required>
                        <button class="btn btn-primary w-100 fw-bold">نشر الحساب</button>
                    </form>
                </div>
                <div class="card p-4">
                    <h6 class="fw-bold mb-3">إضافة سؤال شائع</h6>
                    <form action="/add-faq" method="POST">
                        <input type="text" name="question" class="form-control mb-2" placeholder="السؤال" required>
                        <textarea name="answer" class="form-control mb-2" placeholder="الإجابة" required></textarea>
                        <button class="btn btn-success w-100 fw-bold">إضافة السؤال</button>
                    </form>
                </div>
            </div>
            <div class="col-md-7">
                <div class="card p-4">
                    <h6 class="fw-bold mb-3">قائمة الحسابات</h6>
                    <div class="table-responsive" style="max-height: 400px;">
                        <table class="table table-hover align-middle">
                            <thead class="table-light"><tr><th>ID</th><th>العنوان</th><th>الحالة</th><th>تحكم</th></tr></thead>
                            <tbody>
                                <% accounts.forEach(acc => { %>
                                <tr>
                                    <td>#<%= acc.id %></td>
                                    <td class="small text-truncate" style="max-width: 150px;"><%= acc.title %></td>
                                    <td><span class="badge <%= acc.status === 'متاح' ? 'bg-success' : 'bg-danger' %>"><%= acc.status %></span></td>
                                    <td>
                                        <a href="/toggle-status/<%= acc.id %>" class="btn btn-sm btn-outline-dark"><i class="fas fa-sync"></i></a>
                                        <a href="/delete-account/<%= acc.id %>" class="text-danger ms-2" onclick="return confirm('حذف؟')"><i class="fas fa-trash"></i></a>
                                    </td>
                                </tr>
                                <% }) %>
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>
</html>
