<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= account.title %> | WassitDZ</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.rtl.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        :root { --primary: #ff4d94; --bg: #fff5f8; }
        body { background: var(--bg); font-family: sans-serif; padding-bottom: 50px; }
        
        .main-container { max-width: 800px; margin-top: 20px; }
        
        /* جعل الصورة كبيرة وواضحة جداً */
        .image-container { 
            background: #000; 
            border-radius: 20px; 
            overflow: hidden; 
            box-shadow: 0 10px 30px rgba(0,0,0,0.2);
            margin-bottom: 25px;
            border: 3px solid #fff;
        }
        .img-full { 
            width: 100%; 
            height: auto; 
            display: block;
            object-fit: contain; /* لضمان عدم قص أي جزء من الصورة */
        }

        .details-card { 
            background: #fff; 
            border-radius: 25px; 
            padding: 30px; 
            box-shadow: 0 5px 15px rgba(0,0,0,0.05);
        }

        .info-badge { 
            background: #f8f9fa; 
            padding: 12px 20px; 
            border-radius: 15px; 
            margin-bottom: 15px; 
            border-right: 5px solid var(--primary);
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .price-box { 
            text-align: center; 
            margin: 30px 0; 
            padding: 20px;
            background: var(--bg);
            border-radius: 20px;
        }

        .btn-buy { 
            background: var(--primary); 
            color: #fff; 
            border: none; 
            padding: 15px; 
            border-radius: 15px; 
            font-weight: bold; 
            font-size: 1.2rem;
            transition: 0.3s;
            text-decoration: none;
            display: block;
            text-align: center;
        }
        .btn-buy:hover { background: #e64485; color: #fff; transform: scale(1.02); }
    </style>
</head>
<body>

<div class="container main-container">
    <a href="/" class="btn btn-sm mb-3 text-muted fw-bold">
        <i class="fas fa-arrow-right me-1"></i> العودة للمتجر
    </a>

    <div class="image-container">
        <img src="<%= account.imgs[0] %>" class="img-full" alt="تفاصيل الحساب">
    </div>

    <div class="details-card">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h2 class="fw-bold m-0"><%= account.title %></h2>
            <span class="badge <%= account.status === 'متاح' ? 'bg-success' : 'bg-danger' %> p-2">
                <%= account.status %>
            </span>
        </div>

        <div class="row">
            <div class="col-md-6">
                <div class="info-badge">
                    <span><i class="fas fa-coins text-warning me-2"></i> عدد الكوينز:</span>
                    <strong class="text-dark"><%= account.coins %></strong>
                </div>
            </div>
            <div class="col-md-6">
                <div class="info-badge">
                    <span><i class="fas fa-gem text-info me-2"></i> عدد المجوهرات:</span>
                    <strong class="text-dark"><%= account.gems %></strong>
                </div>
            </div>
        </div>

        <div class="price-box">
            <h1 class="fw-bold text-primary mb-0"><%= account.priceDZ %> دج</h1>
            <p class="text-muted mb-0"><%= account.priceUSD %> دولار أمريكي</p>
        </div>

        <a href="https://wa.me/<%= settings.supportLink %>?text=السلام+عليكم+أريد+شراء+الحساب+رقم+<%= account.id %>" class="btn-buy">
            <i class="fab fa-whatsapp me-2"></i> تواصــل للشراء الآن
        </a>

        <div class="row g-2 mt-3">
            <div class="col-6">
                <a href="<%= settings.mediationLink %>" class="btn btn-outline-secondary w-100 py-2 border-0 bg-light fw-bold text-muted" style="border-radius: 12px;">طلب وساطة</a>
            </div>
            <div class="col-6">
                <a href="<%= settings.sellAccountLink %>" class="btn btn-outline-secondary w-100 py-2 border-0 bg-light fw-bold text-muted" style="border-radius: 12px;">بيع حسابك</a>
            </div>
        </div>
    </div>
</div>

</body>
</html>
