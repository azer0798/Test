<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= account.title %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.rtl.min.css" rel="stylesheet">
    <style>
        :root { --primary: <%= settings.themeColor %>; }
        body { background: #f8fafc; font-family: 'Segoe UI', sans-serif; }
        .main-img { width: 100%; border-radius: 15px; object-fit: contain; max-height: 500px; background: #000; }
        .thumb-img { width: 70px; height: 70px; object-fit: cover; border-radius: 10px; cursor: pointer; border: 2px solid transparent; }
        .thumb-img:hover { border-color: var(--primary); }
        .details-box { background: #fff; border-radius: 20px; padding: 25px; box-shadow: 0 4px 15px rgba(0,0,0,0.05); }
        .btn-order { background: var(--primary); color: #fff; width: 100%; padding: 15px; border-radius: 12px; font-weight: bold; text-decoration: none; display: block; text-align: center; font-size: 1.2rem; }
    </style>
</head>
<body>

<div class="container mt-4">
    <a href="/" class="btn btn-outline-dark mb-4">← العودة للمتجر</a>
    <div class="row g-4">
        <div class="col-md-7">
            <img src="<%= account.imgs[0] %>" id="mainFrame" class="main-img mb-3">
            <div class="d-flex gap-2 overflow-auto">
                <% account.imgs.forEach(img => { %>
                    <img src="<%= img %>" class="thumb-img" onclick="document.getElementById('mainFrame').src='<%= img %>'">
                <% }) %>
            </div>
        </div>
        <div class="col-md-5">
            <div class="details-box">
                <span class="badge bg-primary mb-2"><%= account.linkType %></span>
                <h2 class="fw-bold mb-4"><%= account.title %></h2>
                <div class="mb-2 text-muted">السعر بالعملة المحلية:</div>
                <h3 class="text-success fw-bold mb-3"><%= account.priceDZ %> د.ج</h3>
                <div class="mb-4 text-muted">السعر بالدولار: <span class="fw-bold text-dark"><%= account.priceUSD %> $</span></div>
                <a href="<%= settings.buyNowLink %>" class="btn-order">تواصل لشراء الحساب</a>
            </div>
        </div>
    </div>
</div>

</body>
</html>
