<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>حساب #<%= account.id %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.rtl.min.css" rel="stylesheet">
    <style>
        :root { --primary: <%= settings.themeColor %>; }
        body { background: #f8fafc; font-family: 'Segoe UI', sans-serif; }
        .main-img { width: 100%; border-radius: 15px; cursor: zoom-in; max-height: 500px; object-fit: contain; background: #000; }
        .thumb-img { width: 70px; height: 70px; object-fit: cover; border-radius: 10px; cursor: pointer; margin: 5px; border: 2px solid transparent; }
        .details-box { background: #fff; border-radius: 20px; padding: 25px; box-shadow: 0 4px 15px rgba(0,0,0,0.05); text-align: center; }
        .btn-order { background: var(--primary); color: #fff; width: 100%; padding: 15px; border-radius: 12px; font-weight: bold; text-decoration: none; display: block; }
    </style>
</head>
<body>
    <div class="container mt-4">
        <a href="/" class="btn btn-outline-dark mb-4">← العودة للمتجر</a>
        <div class="row g-4">
            <div class="col-md-7">
                <img src="<%= account.imgs[0] %>" id="main" class="main-img shadow" onclick="this.requestFullscreen()">
                <div class="d-flex overflow-auto mt-2">
                    <% account.imgs.forEach(img => { %>
                        <img src="<%= img %>" class="thumb-img" onclick="document.getElementById('main').src='<%= img %>'">
                    <% }) %>
                </div>
            </div>
            <div class="col-md-5 text-center">
                <div class="details-box">
                    <div class="d-flex justify-content-between mb-3">
                        <span class="badge bg-primary px-3"><%= account.linkType %></span>
                        <span class="text-muted small"><i class="fas fa-eye"></i> <%= account.views %> مشاهدة</span>
                        <h5 class="fw-bold text-primary">#<%= account.id %></h5>
                    </div>
                    <h3 class="fw-bold mb-4"><%= account.title %></h3>
                    <h2 class="text-success fw-bold"><%= account.priceDZ %> د.ج</h2>
                    <p class="text-muted"><%= account.priceUSD %> $</p>
                    <button onclick="handleContact()" class="btn-order border-0 mt-3">اطلب الحساب الآن</button>
                </div>
            </div>
        </div>
    </div>
    <script>
        function handleContact() {
            const link = "<%= settings.buyNowLink %>";
            const msg = "سلام عليكم، أريد شراء الحساب رقم: #<%= account.id %>";
            if (link.includes("wa.me")) window.open(link + "?text=" + encodeURIComponent(msg), "_blank");
            else { navigator.clipboard.writeText(msg); alert("تم نسخ رقم الحساب (#<%= account.id %>)، قم بلصقه عند مراسلتنا"); window.open(link, "_blank"); }
        }
    </script>
</body>
</html>
