<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= account.title %> | #<%= account.id %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.rtl.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        :root { --primary: <%= settings.themeColor %>; }
        body { background: #f8fafc; font-family: 'Segoe UI', sans-serif; }
        .main-img { width: 100%; border-radius: 15px; cursor: zoom-in; max-height: 500px; object-fit: contain; background: #000; }
        .thumb-img { width: 70px; height: 70px; object-fit: cover; border-radius: 10px; cursor: pointer; border: 2px solid transparent; }
        .details-box { background: #fff; border-radius: 20px; padding: 25px; box-shadow: 0 4px 15px rgba(0,0,0,0.05); }
        .btn-order { background: var(--primary); color: #fff; width: 100%; padding: 12px; border-radius: 10px; font-weight: bold; text-decoration: none; display: block; text-align: center; }
    </style>
</head>
<body>
<div class="container mt-4">
    <a href="/" class="btn btn-outline-dark mb-4">← العودة</a>
    <div class="row g-4">
        <div class="col-md-7">
            <img src="<%= account.imgs[0] %>" id="mainFrame" class="main-img mb-3 shadow" title="انقر للتكبير">
            <div class="d-flex gap-2 overflow-auto">
                <% account.imgs.forEach(img => { %>
                    <img src="<%= img %>" class="thumb-img shadow-sm" onclick="document.getElementById('mainFrame').src='<%= img %>'">
                <% }) %>
            </div>
        </div>
        <div class="col-md-5">
            <div class="details-box">
                <div class="d-flex justify-content-between mb-2">
                    <span class="badge bg-primary"><%= account.linkType %></span>
                    <span class="fw-bold text-muted">#<%= account.id %></span>
                </div>
                <h3 class="fw-bold mb-4"><%= account.title %></h3>
                <h2 class="text-success fw-bold mb-2"><%= account.priceDZ %> د.ج</h2>
                <p class="text-muted mb-4"><%= account.priceUSD %> $</p>
                
                <a href="https://wa.me/?text=أريد شراء هذا الحساب من WassitDZ: <%= account.title %> (رقم #<%= account.id %>) السعر: <%= account.priceDZ %> د.ج" 
                   target="_blank" class="btn btn-outline-success w-100 mb-2 fw-bold">
                   <i class="fab fa-whatsapp"></i> مشاركة عبر واتساب
                </a>
                
                <% if(account.status === 'متاح') { %>
                    <a href="<%= settings.buyNowLink %>" class="btn-order">تواصل للشراء الآن</a>
                <% } else { %>
                    <button class="btn btn-danger w-100 fw-bold py-2" disabled>تم بيع هذا الحساب</button>
                <% } %>
            </div>
        </div>
    </div>
</div>
<script>
    document.getElementById('mainFrame').onclick = function() {
        if (this.requestFullscreen) this.requestFullscreen();
        else if (this.webkitRequestFullscreen) this.webkitRequestFullscreen();
    };
</script>
</body>
</html>
