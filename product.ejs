<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <title><%= account.title %> | WassitDZ</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.rtl.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        :root { --pink: #ff4d94; }
        body { background: #fff5f8; font-family: sans-serif; }
        .main-card { background: #fff; border-radius: 20px; padding: 20px; margin-top: 20px; }
        .info-box { background: #f8f9fa; border-radius: 12px; padding: 10px; margin-bottom: 10px; border-right: 4px solid var(--pink); }
    </style>
</head>
<body class="p-3">
    <div class="container main-card shadow-sm">
        <div class="row">
            <div class="col-md-6">
                <img src="<%= account.imgs[0] %>" class="w-100 rounded-4 mb-3" style="max-height: 350px; object-fit: contain; background: #000;">
            </div>
            <div class="col-md-6">
                <span class="badge <%= account.status === 'متاح' ? 'bg-success' : 'bg-danger' %> mb-2"><%= account.status %></span>
                <h2 class="fw-bold mb-3"><%= account.title %></h2>
                
                <div class="info-box">الكوينز: <strong><%= account.coins %></strong></div>
                <div class="info-box">المجوهرات: <strong><%= account.gems %></strong></div>
                
                <div class="h3 fw-bold my-4" style="color: var(--pink);"><%= account.priceDZ %> دج</div>

                <a href="https://wa.me/<%= settings.supportLink %>?text=السلام+عليكم+WassitDZ،+أريد+شراء+الحساب+رقم:+%23<%= account.id %>+<%= account.title %>" 
                   class="btn btn-lg w-100 text-white fw-bold mb-3 shadow-sm" style="background: var(--pink); border-radius: 12px;">
                   شراء الآن
                </a>

                <div class="row g-2">
                    <div class="col-6"><a href="<%= settings.mediationLink %>" class="btn btn-outline-secondary w-100 btn-sm">طلب وساطة</a></div>
                    <div class="col-6"><a href="<%= settings.sellAccountLink %>" class="btn btn-outline-secondary w-100 btn-sm">بيع حسابك</a></div>
                </div>
            </div>
        </div>
    </div>
</body>
</html>
