<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= account.title %> | #<%= account.id %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.rtl.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        :root { --primary: <%= settings.themeColor %>; }
        body { background: #f8fafc; font-family: 'Segoe UI', sans-serif; }
        
        .main-img-wrapper { 
            background: #000; 
            border-radius: 20px; 
            overflow: hidden; 
            box-shadow: 0 10px 30px rgba(0,0,0,0.1);
            position: relative;
        }
        
        .main-img { 
            width: 100%; 
            max-height: 500px; 
            object-fit: contain; 
            cursor: zoom-in; 
            transition: 0.3s;
        }

        .thumb-img { 
            width: 70px; 
            height: 70px; 
            object-fit: cover; 
            border-radius: 12px; 
            cursor: pointer; 
            margin: 5px; 
            border: 2px solid transparent;
            transition: 0.2s;
        }
        .thumb-img:hover { transform: translateY(-3px); border-color: var(--primary); }

        .details-box { 
            background: #fff; 
            border-radius: 25px; 
            padding: 30px; 
            box-shadow: 0 4px 20px rgba(0,0,0,0.05); 
            height: 100%;
        }

        .btn-order { 
            background: var(--primary); 
            color: #fff; 
            width: 100%; 
            padding: 15px; 
            border-radius: 15px; 
            font-weight: 800; 
            font-size: 1.1rem;
            text-decoration: none; 
            display: block; 
            border: none;
            transition: 0.3s;
        }
        .btn-order:hover { 
            filter: brightness(90%); 
            transform: translateY(-2px);
            color: #fff;
        }

        .badge-id { background: #e2e8f0; color: #475569; padding: 5px 15px; border-radius: 10px; font-weight: bold; }
        .price-tag { color: #10b981; font-weight: 900; font-size: 2.5rem; }
    </style>
</head>
<body>

<div class="container mt-4 mb-5">
    <a href="/" class="btn btn-light mb-4 rounded-pill shadow-sm px-4">
        <i class="fas fa-arrow-right me-2"></i> العودة للمتجر
    </a>

    <div class="row g-4">
        <div class="col-lg-7">
            <div class="main-img-wrapper">
                <img src="<%= account.imgs[0] %>" id="mainFrame" class="main-img" title="انقر للتكبير" onclick="toggleFullScreen()">
            </div>
            
            <div class="d-flex justify-content-center flex-wrap mt-3">
                <% account.imgs.forEach((img, index) => { %>
                    <img src="<%= img %>" class="thumb-img shadow-sm" onclick="changeImage('<%= img %>')">
                <% }) %>
            </div>
        </div>

        <div class="col-lg-5">
            <div class="details-box d-flex flex-column justify-content-center">
                <div class="d-flex justify-content-between align-items-center mb-4">
                    <span class="badge bg-primary px-3 py-2 rounded-pill"><%= account.linkType %></span>
                    <span class="badge-id">رقم الحساب: #<%= account.id %></span>
                </div>

                <h2 class="fw-bold mb-3 text-dark"><%= account.title %></h2>
                
                <div class="mb-4">
                    <span class="price-tag"><%= account.priceDZ %></span>
                    <span class="text-success fw-bold"> د.ج</span>
                    <div class="text-muted fs-5"><%= account.priceUSD %> $</div>
                </div>

                <hr class="my-4 text-muted">

                <div class="features mb-4">
                    <div class="d-flex align-items-center mb-2">
                        <i class="fas fa-check-circle text-success me-2"></i>
                        <span>تسليم فوري وآمن</span>
                    </div>
                    <div class="d-flex align-items-center mb-2">
                        <i class="fas fa-eye text-muted me-2"></i>
                        <span class="small text-muted">تمت مشاهدة هذا الحساب <%= account.views %> مرة</span>
                    </div>
                </div>

                <button onclick="handleContact()" class="btn-order shadow">
                    <i class="fab fa-whatsapp me-2"></i> اطلب الحساب الآن
                </button>
                
                <p class="text-center mt-3 small text-muted">
                    عند الضغط، سيتم إرسال رقم الحساب #<%= account.id %> تلقائياً للوسيط
                </p>
            </div>
        </div>
    </div>
</div>

<script>
    // وظيفة تغيير الصورة الرئيسية
    function changeImage(src) {
        document.getElementById('mainFrame').src = src;
    }

    // وظيفة تكبير الصورة (ملء الشاشة)
    function toggleFullScreen() {
        const img = document.getElementById('mainFrame');
        if (img.requestFullscreen) img.requestFullscreen();
        else if (img.webkitRequestFullscreen) img.webkitRequestFullscreen();
    }

    // وظيفة التواصل الذكية
    function handleContact() {
        const link = "<%= settings.buyNowLink %>";
        const accountId = "#<%= account.id %>";
        const message = "سلام عليكم، أريد شراء الحساب رقم: " + accountId;

        if (link.includes("wa.me") || link.includes("whatsapp")) {
            // واتساب: إضافة النص للرابط مباشرة
            const cleanLink = link.split('?')[0];
            window.open(cleanLink + "?text=" + encodeURIComponent(message), "_blank");
        } else {
            // تلغرام أو غيره: نسخ النص وفتح الرابط
            navigator.clipboard.writeText(message).then(() => {
                alert("تم نسخ رقم الحساب (#<%= account.id %>) تلقائياً، قم بلصقه عند مراسلتنا.");
                window.open(link, "_blank");
            });
        }
    }
</script>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

</body>
</html>
