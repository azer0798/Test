<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= account.title %> | WassitDZ</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.rtl.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Cairo:wght@400;700&display=swap" rel="stylesheet">
    <style>
        :root { --primary: #ff4d94; --bg: #fff5f8; }
        body { background: var(--bg); font-family: 'Cairo', sans-serif; }
        .product-card { background: #fff; border-radius: 20px; border: 2px solid #ffeef4; overflow: hidden; }
        .main-img { width: 100%; height: 350px; object-fit: contain; background: #000; }
        .thumb-img { width: 80px; height: 80px; object-fit: cover; cursor: pointer; border-radius: 10px; border: 2px solid #eee; }
        .thumb-img:hover { border-color: var(--primary); }
        .price-box { background: #fff0f5; padding: 15px; border-radius: 15px; border: 1px dashed var(--primary); }
        .stat-badge { background: #f8f9fa; padding: 10px; border-radius: 12px; border: 1px solid #eee; text-align: center; }
        .btn-buy { background: var(--primary); color: #fff; border-radius: 12px; padding: 12px; font-weight: bold; width: 100%; text-decoration: none; display: block; text-align: center; transition: 0.3s; }
        .btn-buy:hover { opacity: 0.9; color: #fff; transform: translateY(-3px); }
    </style>
</head>
<body class="p-3 p-md-5">

    <div class="container">
        <div class="mb-4">
            <a href="/" class="text-decoration-none" style="color: var(--primary);"><i class="fas fa-arrow-right me-2"></i> العودة للرئيسية</a>
        </div>

        <div class="row g-4">
            <div class="col-lg-6">
                <div class="product-card shadow-sm">
                    <img src="<%= account.imgs[0] %>" id="currentImg" class="main-img">
                    <div class="p-3 d-flex gap-2 overflow-auto">
                        <% account.imgs.forEach(img => { %>
                            <img src="<%= img %>" class="thumb-img" onclick="document.getElementById('currentImg').src='<%= img %>'">
                        <% }) %>
                    </div>
                </div>
            </div>

            <div class="col-lg-6">
                <div class="product-card p-4 shadow-sm">
                    <span class="badge mb-2 <%= account.status === 'متاح' ? 'bg-success' : 'bg-danger' %>"><%= account.status %></span>
                    <h2 class="fw-bold mb-3"><%= account.title %></h2>
                    
                    <div class="row g-2 mb-4">
                        <div class="col-6">
                            <div class="stat-badge">
                                <small class="text-muted d-block">الكوينز</small>
                                <span class="fw-bold"><%= account.coins || 'غير محدد' %></span>
                            </div>
                        </div>
                        <div class="col-6">
                            <div class="stat-badge">
                                <small class="text-muted d-block">المجوهرات</small>
                                <span class="fw-bold"><%= account.gems || 'غير محدد' %></span>
                            </div>
                        </div>
                    </div>

                    <div class="price-box mb-4 text-center">
                        <h3 class="fw-bold mb-1" style="color: var(--primary);"><%= account.priceDZ %> دج</h3>
                        <span class="text-muted">ما يعادل: <%= account.priceUSD %> دولار</span>
                    </div>

                    <div class="d-grid gap-2">
                        <a href="<%= settings.supportLink %>" class="btn-buy">
                            <i class="fab fa-whatsapp me-2"></i> شراء الحساب الآن
                        </a>
                        
                        <div class="row g-2 mt-2">
                            <div class="col-6">
                                <a href="<%= settings.mediationLink %>" class="btn btn-outline-secondary w-100" style="border-radius:10px; font-size:14px;">طلب وساطة</a>
                            </div>
                            <div class="col-6">
                                <a href="<%= settings.sellAccountLink %>" class="btn btn-outline-secondary w-100" style="border-radius:10px; font-size:14px;">بيع حسابك</a>
                            </div>
                        </div>
                    </div>

                    <div class="mt-4 pt-3 border-top text-muted small">
                        <i class="fas fa-eye me-1"></i> تمت مشاهدة هذا الحساب <%= account.views %> مرة
                    </div>
                </div>
            </div>
        </div>
    </div>

</body>
</html>
