<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <title><%= account.title %> | WassitDZ</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.rtl.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        :root { --primary: #ff4d94; }
        body { background: #fff5f8; font-family: sans-serif; }
        .product-card { background: white; border-radius: 20px; padding: 25px; margin-top: 30px; }
        .img-main { width: 100%; height: 350px; object-fit: contain; background: #000; border-radius: 15px; }
        .info-item { background: #f8f9fa; padding: 10px; border-radius: 10px; margin-bottom: 10px; border-right: 4px solid var(--primary); }
    </style>
</head>
<body class="p-3">
    <div class="container product-card shadow-sm">
        <a href="/" class="text-decoration-none" style="color: var(--primary);"><i class="fas fa-arrow-right"></i> العودة</a>
        <div class="row mt-3">
            <div class="col-md-6 text-center">
                <img src="<%= account.imgs[0] %>" class="img-main mb-3">
            </div>
            <div class="col-md-6">
                <h3 class="fw-bold mb-3"><%= account.title %></h3>
                <div class="info-item">عدد الكوينز: <strong><%= account.coins %></strong></div>
                <div class="info-item">عدد المجوهرات: <strong><%= account.gems %></strong></div>
                <div class="my-4 text-center">
                    <h2 class="fw-bold text-primary"><%= account.priceDZ %> دج</h2>
                    <span class="text-muted"><%= account.priceUSD %> دولار</span>
                </div>
                <a href="<%= settings.supportLink %>" class="btn btn-primary w-100 py-3 fw-bold mb-3" style="background: var(--primary); border: none; border-radius: 12px;">تواصل للشراء</a>
                <div class="row g-2">
                    <div class="col-6"><a href="<%= settings.mediationLink %>" class="btn btn-outline-secondary w-100">طلب وساطة</a></div>
                    <div class="col-6"><a href="<%= settings.sellAccountLink %>" class="btn btn-outline-secondary w-100">بيع حسابك</a></div>
                </div>
            </div>
        </div>
    </div>
</body>
</html>
